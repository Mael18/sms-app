{
  "name": "family-sms-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Small relay + web UI for sending SMS through an SMS Gate cloud device.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
